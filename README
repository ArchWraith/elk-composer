WINDOWS INSTRUCTIONS
STEP 1: INSTALL DOCKER
- if you have internet access, it is recommended that the latest version of the docker installer be downloaded from https://docs.docker.com/desktop/windows/install/
- if you do not have internet access, please use the installer that is provided "Docker Desktop Installer.exe"
- restart the system
- Docker does not work without wsl 2 so that needs to be installed and enabled on the host machine
- wsl 2 can be enabled with this command "wsl --set-default-version 2"

STEP 2: LOAD THE CONTAINERS
- run the "get_images.bat" script to load the containers into your docker instance

STEP 3: LOAD THE PIPELINE
- this has not been figured out yet

STEP 4: DOCKER COMPOSE
- run "compose.bat"
- logs can be viewed with the command "docker-compose logs -f"
  
